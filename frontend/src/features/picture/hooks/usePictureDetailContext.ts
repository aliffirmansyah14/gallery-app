// import type { PictureDetailContextTypes } from "@/providers/PictureDetail";
// import { createContext, use } from "react";

// export const PictureDetailContext = createContext<
// 	PictureDetailContextTypes | undefined
// >(undefined);

// export const usePictureDetailContext = (): PictureDetailContextTypes => {
// 	const context = use(PictureDetailContext);
// 	if (!context) throw new Error("useFile digunakan harus didalam FileProvider");

// 	return context;
// };
